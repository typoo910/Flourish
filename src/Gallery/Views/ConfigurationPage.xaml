<Page
  x:Class="ArkheideSystem.Gallery.Views.ConfigurationPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:flourish="http://schemas.arkheide.system/flourish"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Background="Transparent"
>
  <flourish:FlourishScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="{DynamicResource FlourishPageContentMargin}">
      <flourish:ChunkHero
        ChunkHeroDescription="Edit configuration and localization while the app runs."
        ChunkHeroTitle="Configuration &amp; localization"
      />

      <flourish:Chunk
        ChunkDescription="Read values from the active provider chain."
        ChunkTitle="Effective configuration"
      >
        <UniformGrid Columns="2">
          <flourish:Card
          Margin="0,0,8,0"
          Title="Merged value"
          Text="The configuration service exposes the effective value from every registered provider."
        >
          <flourish:Card.Body>
            <StackPanel>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <flourish:FlourishTextBox x:Name="ReadKeyBox" Text="Flourish:Preferences:Theme" />
              <flourish:Button Grid.Column="1" Margin="10,0,0,0" Click="ReadValue_Click" Content="Read value" />
              <flourish:Button Grid.Column="2" Margin="8,0,0,0" Click="Reload_Click" Content="Reload providers" />
            </Grid>
            </StackPanel>
          </flourish:Card.Body>
        </flourish:Card>
          <flourish:Card Margin="8,0,0,0" Title="Result" Text="Shows the resolved value and provider snapshot.">
            <flourish:Card.Body>
              <StackPanel>
                <flourish:FlourishTextBlock x:Name="ReadValueText" Role="Status" Text="No value read yet." />
                <flourish:FlourishTextBlock x:Name="SnapshotText" Margin="0,8,0,0" Role="Status" />
              </StackPanel>
            </flourish:Card.Body>
          </flourish:Card>
        </UniformGrid>
      </flourish:Chunk>

      <flourish:Chunk
        ChunkDescription="Edit appsettings.json atomically."
        ChunkTitle="Transactional appsettings update"
      >
        <UniformGrid Columns="2">
          <flourish:Card
          Margin="0,0,8,0"
          Title="File transaction"
          Text="Set, append, and remove use atomic replacement; successful writes immediately reload effective configuration."
        >
          <flourish:Card.Body>
            <StackPanel>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <StackPanel Margin="0,0,8,0">
                <flourish:FlourishTextBlock Role="FieldLabel" Text="Configuration path" />
                <flourish:FlourishTextBox x:Name="WriteKeyBox" Text="Gallery:Runtime:Message" />
              </StackPanel>
              <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <flourish:FlourishTextBlock Role="FieldLabel" Text="String value" />
                <flourish:FlourishTextBox x:Name="WriteValueBox" Text="Hello from runtime" />
              </StackPanel>
            </Grid>
            <WrapPanel Margin="0,14,0,0">
              <flourish:Button Margin="0,0,8,0" Click="SetValue_Click" Content="Set &amp; save" />
              <flourish:Button Margin="0,0,8,0" Click="MergeValue_Click" Content="Merge into parent" />
              <flourish:Button Margin="0,0,8,0" Click="AppendValue_Click" Content="Append to array" />
              <flourish:Button Click="RemoveValue_Click" Content="Remove" />
            </WrapPanel>
            </StackPanel>
          </flourish:Card.Body>
        </flourish:Card>
          <flourish:Card Margin="8,0,0,0" Title="Result" Text="Shows the latest file transaction outcome.">
            <flourish:Card.Body>
              <flourish:FlourishTextBlock x:Name="WriteResultText" Role="Status" Text="No runtime update yet." />
            </flourish:Card.Body>
          </flourish:Card>
        </UniformGrid>
      </flourish:Chunk>

      <flourish:Chunk
        ChunkDescription="Switch the active localization source."
        ChunkTitle="Runtime locale"
      >
        <UniformGrid Columns="2">
          <flourish:Card
          Margin="0,0,8,0"
          Title="Locale selection"
          Text="Choose a built-in locale or enter the identifier of a locale registered by the host."
        >
          <flourish:Card.Body>
            <StackPanel>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <flourish:FlourishComboBox
                x:Name="LocaleBox"
                IsEditable="True"
              />
              <flourish:Button Grid.Column="1" Margin="10,0,0,0" Click="ApplyLocale_Click" Content="Apply locale" />
            </Grid>
            </StackPanel>
          </flourish:Card.Body>
        </flourish:Card>
          <flourish:Card Margin="8,0,0,0" Title="Output" Text="Shows the effective locale after each request.">
            <flourish:Card.Body>
              <flourish:FlourishTextBlock x:Name="LocaleStatusText" Role="Status" />
            </flourish:Card.Body>
          </flourish:Card>
        </UniformGrid>
      </flourish:Chunk>

      <flourish:Chunk
        ChunkDescription="Manage an external locale file."
        ChunkTitle="Runtime locale files"
      >
        <UniformGrid Columns="2">
          <flourish:Card
          Margin="0,0,8,0"
          Title="Locale registration"
          Text="Register a lang_&lt;locale&gt;.json file, reload external edits, or remove the source through its registration handle."
        >
          <flourish:Card.Body>
            <StackPanel>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <flourish:FlourishTextBox x:Name="LocaleFilePathBox" Text="lang_ES.json" />
              <flourish:Button Grid.Column="1" Margin="10,0,0,0" Click="RegisterLocaleFile_Click" Content="Register file" />
              <flourish:Button Grid.Column="2" Margin="8,0,0,0" Click="ReloadLocaleFile_Click" Content="Reload file" />
              <flourish:Button Grid.Column="3" Margin="8,0,0,0" Click="UnregisterLocaleFile_Click" Content="Unregister" />
            </Grid>
            </StackPanel>
          </flourish:Card.Body>
        </flourish:Card>
          <flourish:Card Margin="8,0,0,0" Title="Result" Text="Shows the current file registration state.">
            <flourish:Card.Body>
              <flourish:FlourishTextBlock x:Name="LocaleFileStatusText" Role="Status" Text="No locale file registered by this page." />
            </flourish:Card.Body>
          </flourish:Card>
        </UniformGrid>
      </flourish:Chunk>
    </StackPanel>
  </flourish:FlourishScrollViewer>
</Page>
