<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="clr-namespace:ArkheideSystem.Flourish.Controls"
>
  <Style TargetType="{x:Type controls:FlourishRadioButton}">
    <Setter Property="MinHeight" Value="{DynamicResource FlourishControlHeight}" />
    <Setter Property="Foreground" Value="{DynamicResource FlourishNeutralForeground1Brush}" />
    <Setter Property="FontFamily" Value="{DynamicResource FlourishFontFamily}" />
    <Setter Property="FontSize" Value="{DynamicResource FlourishFontSizeStandard}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type controls:FlourishRadioButton}">
          <Grid Background="Transparent" SnapsToDevicePixels="True">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border
              x:Name="RadioChrome"
              Width="16"
              Height="16"
              VerticalAlignment="Center"
              Background="{DynamicResource FlourishControlBackgroundBrush}"
              BorderBrush="{DynamicResource FlourishControlStrokeBrush}"
              BorderThickness="{DynamicResource FlourishControlBorderThickness}"
              CornerRadius="8"
            >
              <Ellipse
                x:Name="SelectionDot"
                Width="8"
                Height="8"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Fill="{DynamicResource FlourishForegroundOnPrimaryBrush}"
                Visibility="Collapsed"
              />
            </Border>
            <ContentPresenter
              x:Name="ContentHost"
              Grid.Column="1"
              Margin="8,0,0,0"
              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
              Content="{TemplateBinding Content}"
              ContentStringFormat="{TemplateBinding ContentStringFormat}"
              ContentTemplate="{TemplateBinding ContentTemplate}"
              ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
              RecognizesAccessKey="True"
            />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="RadioChrome" Property="Background" Value="{DynamicResource FlourishControlHoverBrush}" />
              <Setter TargetName="RadioChrome" Property="BorderBrush" Value="{DynamicResource FlourishControlStrokeHoverBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="RadioChrome" Property="Background" Value="{DynamicResource FlourishControlPressedBrush}" />
              <Setter TargetName="RadioChrome" Property="BorderBrush" Value="{DynamicResource FlourishControlStrokePressedBrush}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="RadioChrome" Property="Background" Value="{DynamicResource FlourishPrimaryBackgroundBrush}" />
              <Setter TargetName="RadioChrome" Property="BorderBrush" Value="{DynamicResource FlourishPrimaryBackgroundBrush}" />
              <Setter TargetName="SelectionDot" Property="Visibility" Value="Visible" />
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsChecked" Value="True" />
                <Condition Property="IsMouseOver" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="RadioChrome" Property="Background" Value="{DynamicResource FlourishPrimaryBackgroundHoverBrush}" />
              <Setter TargetName="RadioChrome" Property="BorderBrush" Value="{DynamicResource FlourishPrimaryBackgroundHoverBrush}" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsChecked" Value="True" />
                <Condition Property="IsPressed" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="RadioChrome" Property="Background" Value="{DynamicResource FlourishPrimaryBackgroundPressedBrush}" />
              <Setter TargetName="RadioChrome" Property="BorderBrush" Value="{DynamicResource FlourishPrimaryBackgroundPressedBrush}" />
            </MultiTrigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="RadioChrome" Property="BorderBrush" Value="{DynamicResource FlourishControlStrokeFocusBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource FlourishNeutralForegroundDisabledBrush}" />
              <Setter Property="Cursor" Value="Arrow" />
              <Setter TargetName="RadioChrome" Property="Background" Value="{DynamicResource FlourishControlDisabledBrush}" />
              <Setter TargetName="RadioChrome" Property="BorderBrush" Value="{DynamicResource FlourishControlStrokeDisabledBrush}" />
              <Setter TargetName="SelectionDot" Property="Fill" Value="{DynamicResource FlourishNeutralForegroundDisabledBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
