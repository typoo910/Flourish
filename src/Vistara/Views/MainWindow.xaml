<?xml version="1.0" encoding="utf-8" ?>
<Window
  x:Class="Vistara.Views.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="using:Vistara.Views"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Title="MainWindow"
  mc:Ignorable="d"
>
  <Window.SystemBackdrop>
    <MicaBackdrop />
  </Window.SystemBackdrop>

  <Grid>
    <!--  Structure  -->
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <!--  End Structure  -->

    <!--  Title Bar  -->
    <TitleBar
      x:Name="AppTitleBar"
      Title="Vistara"
      Grid.Row="0"
      BackRequested="TitleBar_BackRequested"
      IsBackButtonEnabled="{x:Bind RootFrame.CanGoBack, Mode=OneWay}"
      IsBackButtonVisible="True"
      IsPaneToggleButtonVisible="True"
      PaneToggleRequested="TitleBar_PaneToggleRequested"
      Subtitle="Image Manager"
    >
      <TitleBar.IconSource>
        <ImageIconSource
          x:Name="AppIcon"
          ImageSource="ms-appx:///Assets/Square44x44Logo.scale-200.png"
        />
      </TitleBar.IconSource>
      <TitleBar.Content>
        <AutoSuggestBox
          MinWidth="300"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Center"
          PlaceholderText="What are you looking for?"
          QueryIcon="Find"
        />
      </TitleBar.Content>
      <TitleBar.RightHeader>
        <PersonPicture Width="30" Initials="JD" />
      </TitleBar.RightHeader>
    </TitleBar>
    <!--  End Title Bar  -->

    <!--  Action Buttons  -->
    <Border
      Grid.Row="1"
      Margin="12,0,12,6"
      Padding="6"
      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
      BorderThickness="1"
      CornerRadius="8"
    >
      <Border.Shadow>
        <ThemeShadow />
      </Border.Shadow>

      <StackPanel Orientation="Horizontal" Spacing="6">
        <Button Padding="7,5">
          <StackPanel Orientation="Horizontal" Spacing="6">
            <FontIcon Glyph="&#xE8E5;" />
            <TextBlock Text="打开" />
          </StackPanel>
        </Button>

        <Button Padding="7,5">
          <StackPanel Orientation="Horizontal" Spacing="6">
            <FontIcon Glyph="&#xE74E;" />
            <TextBlock Text="保存" />
          </StackPanel>
        </Button>

        <Button Padding="7,5">
          <StackPanel Orientation="Horizontal" Spacing="6">
            <FontIcon Glyph="&#xE898;" />
            <TextBlock Text="导入" />
          </StackPanel>
        </Button>
      </StackPanel>
    </Border>
    <!--  End Action Buttons  -->

    <!--  Content  -->
    <Border
      Grid.Row="2"
      Margin="12,0,12,6"
      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
      BorderThickness="1"
      CornerRadius="8"
    >
      <NavigationView
        x:Name="RootNavigationView"
        AlwaysShowHeader="False"
        CompactPaneLength="48"
        IsBackButtonVisible="Collapsed"
        IsPaneOpen="True"
        IsPaneToggleButtonVisible="False"
        IsSettingsVisible="False"
        OpenPaneLength="220"
        PaneDisplayMode="Left"
        SelectionChanged="RootNavigationView_SelectionChanged"
      >
        <NavigationView.MenuItems>
          <NavigationViewItem x:Name="HomeItem" Content="首页" Tag="Home">
            <NavigationViewItem.Icon>
              <SymbolIcon Symbol="Home" />
            </NavigationViewItem.Icon>
          </NavigationViewItem>
          <NavigationViewItem x:Name="GalleryItem" Content="图库" Tag="Gallery">
            <NavigationViewItem.Icon>
              <SymbolIcon Symbol="Pictures" />
            </NavigationViewItem.Icon>
          </NavigationViewItem>
          <NavigationViewItem x:Name="EditorItem" Content="编辑" Tag="Editor">
            <NavigationViewItem.Icon>
              <SymbolIcon Symbol="Edit" />
            </NavigationViewItem.Icon>
          </NavigationViewItem>
          <NavigationViewItem x:Name="SettingsItem" Content="设置" Tag="Settings">
            <NavigationViewItem.Icon>
              <SymbolIcon Symbol="Setting" />
            </NavigationViewItem.Icon>
          </NavigationViewItem>
        </NavigationView.MenuItems>

        <Frame x:Name="RootFrame" />
      </NavigationView>
    </Border>
    <!--  End Content  -->

    <Border
      Grid.Row="3"
      Padding="12,10"
      Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
      BorderThickness="1,1,1,0"
    >
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock VerticalAlignment="Center" Text="就绪" />

        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="20">
          <StackPanel Orientation="Horizontal" Spacing="6">
            <FontIcon Glyph="&#xE823;" />
            <TextBlock Text="12:45" />
          </StackPanel>

          <StackPanel Orientation="Horizontal" Spacing="6">
            <FontIcon Glyph="&#xE701;" />
            <TextBlock Text="已连接" />
          </StackPanel>

          <StackPanel Orientation="Horizontal" Spacing="6">
            <FontIcon Glyph="&#xE850;" />
            <TextBlock Text="82%" />
          </StackPanel>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Window>
